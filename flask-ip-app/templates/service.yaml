apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-ip-app.fullname" . }}
  labels:
    {{- include "flask-ip-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-ip-app.selectorLabels" . | nindent 4 }}
